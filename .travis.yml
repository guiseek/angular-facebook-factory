language: node_js
node_js:
  6.9.2
install:
  mkdir dist
  npm install
before_deploy:
  npm run build
deploy:
  provider: releases
  api_key:
    secure: $GITHUB_TOKEN
  file:
    - "dist/bundle.js"
    - "dist/bundle.min.js"
  skip_cleanup: true
  overwrite: true
  on:
    tags: true